// Thin shim — re-export the unified SpreadsheetGrid from cursedbelt.
// The lazy/Suspense wrapper lives in index.tsx (same as before).
export { SpreadsheetGrid as default } from 'cursedbelt/react/spreadsheet';
