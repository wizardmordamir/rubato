// Public barrel for the embeddable shell — the `rubato/ui/shell` lib entry.
export { AppShell, type AppShellProps } from "./AppShell";
export { AppProviders, type AppProvidersProps } from "./AppProviders";
