// Public barrel for the automations pages — the `rubato/ui/automations` lib entry.
// The page components live one level up alongside the other rubato pages; this
// re-export packages just the automations trio for friend apps.
export { AutomationsPage } from "../AutomationsPage";
export { BuilderPage } from "../BuilderPage";
export { ViewAutomationPage } from "../ViewAutomationPage";
