/**
 * Splunk: the query builder (pure string assembly from app config) plus a search
 * client that executes a built query for users who've configured Splunk keys.
 */

export * from './client';
export * from './queryBuilder';
