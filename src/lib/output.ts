/**
 * Aligned-table and RFC-4180 CSV rendering — re-exported from `cwip` (its
 * canonical home; the implementation was promoted there from this file). Kept
 * as the entry behind the `rubato/output` public subpath.
 */

export { type Row, toCsv, toTable } from 'cwip';
